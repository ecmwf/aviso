.. _echo_examples:

Echo
=================

Below find an example of an event listener for ``flight`` events that will execute a ``echo`` trigger 
in case of notifications.

.. literalinclude:: echo_listener.yaml
   :language: yaml
