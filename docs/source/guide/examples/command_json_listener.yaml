listeners:
  - event: dissemination
    request:
      destination: FOO
      class: od
      expver: 1
      domain: g
    triggers:
      - type: command
        command: examples/script.sh --json ${json}
