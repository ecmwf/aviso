listeners:
  - event: dissemination
    request:
      destination: FOO
      stream: enfo
    triggers:
      - type: command
        command: tests/integration/fixtures/wrongPath.sh
        environment:
          STEP: ${request.step}
          TIME: "The time is ${request.time}"