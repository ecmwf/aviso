listeners:
  - event: dissemination
    request:
      destination: FOO
      stream: enfo
    triggers:
      - type: echo
      - type: command
        command: echoerror "AAAAA"
        environment:
          STEP: ${request.step}
          TIME: "The time is ${request.time}"
      - type: log
        path: testLog.log