listeners:
  - event: dissemination
    request:
      destination: FOO
      stream: [enfo]
      date: [201981]
      time: [06]
    triggers:
      - type: command
        command: echo "AAAAA"
        environment:
          STEP: ${request.step}
          TIME: "The time is ${request.time}"

