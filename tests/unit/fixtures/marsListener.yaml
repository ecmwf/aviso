listeners:
  - event: mars
    request:
      stream: enfo
      date: 20190801
      time: [06]
    triggers:
      - type: command
        command: echo "AAAAA"
        environment:
          STEP: ${request.step}
          TIME: "The time is ${request.time}"

