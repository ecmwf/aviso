listeners:
  - event: dissemination
    request:
      destination: ZAG
      stream: enfo
    triggers:
      - type: command
        command: python examples/pythonScript.py
        environment:
          STEP: ${request.step}
          TIME: "The time is ${request.time}"

